<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codepath.appointsy">
    <!-- allow our app access to the internet -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ParseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appointsy"
        android:usesCleartextTraffic="true">
        <activity android:name=".CreateAppointmentActivity"/>
        <activity android:name=".BusinessDetails"/>
        <activity android:name=".AppointmentViewActivity" />
        <activity android:name=".BusinessProfileActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Appointsy.NoActionBar">

            <!-- add the MAIN & LAUNCHER here -->

        </activity>
    </application>

</manifest>